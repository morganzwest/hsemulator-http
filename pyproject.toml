[project]
name = "novocode-runtime-api"
version = "0.1.0"
description = "Novocode Runtime API"
requires-python = ">=3.11"

dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "pydantic",
  "python-dotenv",
  "supabase"
]

[tool.uvicorn]
factory = false
